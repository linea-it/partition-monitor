import { makeStyles } from '@material-ui/core/styles';

const styles = makeStyles({
  input : {
    margin: '-5px',
    color: 'rgb(68, 68, 68)',
    cursor: 'text',
    fontSize: '1rem',
    display: 'inline-flex',
  },
  inputDayPicker : {
    
  },
  textField : {
    pointerEvents: 'none',
  },
  margin : {
    margin: '15px',
  },
});

export default styles;